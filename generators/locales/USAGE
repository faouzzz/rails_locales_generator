Description:
    rails 3+ generator to create locales files for active record models 
    and action view helpers (submit & button)
    
    If you haven't already read: http://guides.rubyonrails.org/i18n.html

Example:
    rails generate locales campaigns

    This will create:
        config/locales/models/campaigns/en.yml
        config/locales/views/campaigns/en.yml
